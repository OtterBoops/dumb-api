import '../../styles/containers/Header.scss'

import React from 'react';
import { Box } from '@material-ui/core';

function Header() {
    return (
        <Box maxWidth="xl" className="Header">
            <p>Header</p>
        </Box>
    );
}

export default Header;